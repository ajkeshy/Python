# 🤖 Python Robot Controller

A simple command-line interface (CLI) robot simulator built with Python.

## 🚀 Features
* Accepts user input to control a virtual robot.
* specific commands like `start`, `stop`, `dance`, and `charge`.
* **Input Sanitization:** Automatically handles uppercase/lowercase text.
* **Scalable Design:** Uses a Python Dictionary to map commands, making it easy to add new features.

## 🛠️ How to Run
1. Install Python 3.x
2. Clone this repository.
3. Run the script:
   ```bash
   python "Bot Controller.py"

## What I Learned
1. Python dictionaries for data mapping
2. While loops for continuous execution.
3. Conditional logic for system exits.